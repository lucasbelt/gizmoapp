<%= render 'shared/page_title', title: "showing all recipes" %>

<div class="list-recipe">
  <ul>
    <%= render 'recipe', obj: @recipes %>
  </ul>

  <%= will_paginate @recipes, renderer: WillPaginateInfinite::InfinitePagination %>
</div>